#include "deck.h"
#include <string>

void Deck::loadDeck(std::ifstream &infile)
{
    std::string name;
    while (std::getline(infile, name))
    {
        if (name == "Air Elemental")
        {
        }
    }
}
